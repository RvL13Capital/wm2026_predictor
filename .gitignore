__pycache__/
*.py[cod]
*$py.class

# Environments
venv/
env/
ENV/
env.bak/
venv.bak/

# Project files
.DS_Store

# Temp files
tmp*.csv
temp_*.csv
data/run_test_mc.txt
data/vectorized_run_*.txt

# Data outputs generated locally
data/*_tips*.txt
data/bonusfragen*.txt
data/bonusfragen*.json
data/polymarket*.json
data/context_calibration_log.jsonl

# Agent scratch / temp files (never commit)
/test_points.py
/tmp_out.txt

# Runtime outputs (transient stdout/test dumps; curated reports live in validation/*.txt)
*_out.txt

# AI agent scratchpads — kept in git history for provenance, untracked from the working tree
.agents/

# Generated tip-sheet PDFs from make_tips_pdf.py (the curated report lives in report/)
von_linck_capital_*.pdf

# Generated Golden Boot ranking (goalscorer.py)
data/goldenboot.json

# Precomputed matrix cache (S13) — regenerated on demand, fingerprint-keyed
data/matrix_cache/
